# Runner

This folder will contain the orchestration layer that executes workers, batches inputs, and coordinates the comparator and reporter.

Planned responsibilities:
- Manage worker lifecycles.
- Batch inputs to reduce overhead.
- Capture worker logs and outputs.
