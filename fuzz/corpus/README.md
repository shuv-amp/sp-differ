# Corpus

This folder stores seed and discovered cases for fuzzing.

- `semantic_worker/` is the checked-in seed corpus for semantic worker ABI fuzzing.
- The corpus is deterministic and regenerated by `python3 scripts/generate_semantic_fuzz_corpus.py`.
- Failure artifacts belong under `build/`, not in this tracked corpus.
