# Output Example (v1)

This example shows a minimal successful output payload. It corresponds to a worker run that validates the case format but does not derive any recipient keys.

## Summary

- `version`: `1`
- `status`: `ok` (`0`)
- `output_count`: `0`

## Hex Encoding

The file `tests/vectors/output_ok.hex` contains the raw hex payload below.

```text
01000000
```
