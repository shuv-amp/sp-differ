# Vendored Upstream Reference Bundle

This directory contains the exact upstream Python files needed to run the official BIP 352 reference implementation offline from this repository.

The file list and SHA256 values are recorded in `tests/vectors/bip352/official/manifest.json`.

Do not edit these files by hand. Refresh them from the pinned upstream commit with `python3 scripts/fetch_bip352_vectors.py`.
