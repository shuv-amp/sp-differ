# Regression Cases

This folder will store minimal cases that reproduce mismatches found during fuzzing or review. Each case should include metadata describing origin, commit hashes, and expected behavior.
