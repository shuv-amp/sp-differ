# Regression Cases

This folder stores minimal cases that reproduce mismatches found during fuzzing or review. Cases may be pinned `.hex` inputs or exact semantic `request.json` payloads when the minimized reproducer no longer maps cleanly to a single source case. Each retained case should include metadata describing origin, commit hashes, and expected behavior.
